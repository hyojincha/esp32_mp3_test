#
# Main Makefile. This is basically the same as a component makefile.
#

COMPONENT_ADD_INCLUDEDIRS += codec
COMPONENT_ADD_INCLUDEDIRS += libs/include
COMPONENT_ADD_INCLUDEDIRS += mp3_decoder
COMPONENT_ADD_INCLUDEDIRS += mp3player

COMPONENT_SRCDIRS += codec
COMPONENT_SRCDIRS += libs/src
COMPONENT_SRCDIRS += mp3_decoder
COMPONENT_SRCDIRS += mp3player

